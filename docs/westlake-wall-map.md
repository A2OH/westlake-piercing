---
name: westlake-wall-map
description: "Ranked roadmap of noice's startup walls (cleared/active/predicted) + the strategic insight that remaining walls cluster into ~3 root efforts. Lead with source analysis, not per-wall empiricism."
metadata: 
  node_type: memory
  type: project
  originSessionId: 422676c7-2c64-4e1a-8602-735c79264cef
---

**Strategy (2026-06-02, after the theme rabbit hole):** discover walls by SOURCE analysis (app startup path vs adapter gaps), batch-fix by ROOT, verify on-device second. The empirical one-wall-at-a-time loop is slow + measurement-trap-prone (see [[measurement-channels]]). Walls are mostly in the ADAPTER (substrate C++/Java), not AOSP.

**noice startup path → walls (in order):**
1. **Spawn (appspawn-x fork)** — PARTIAL/flaky: forks reliably only ~1st launch post-reboot; AMS drops rapid relaunches; `kill -9 helloworld` disrupts appspawn (→ dual-appspawn). Cross-cutting; slows all testing.
2. **initChild + bind (`handleBindApplication`)** — ✅ CLEARED (post-reboot boot-scan; AES probe benign).
3. **Splash/launch gate (launchMode→SPECIFIED, AcceptWant)** — ✅ CLEARED (launchMode-map in libapk_installer `3d4d9d5f` + B49 `AddAbilityStageDone` bridge `8bb53fc8`).
4. **W20 (LinkMethods bad_alloc, `ss_lists` cycle in FIX-VTABLE-A)** — ✅ FIXED+VERIFIED (libart cycle guard `56f3caea`; banked `W20-OPERATOR-HANDOFF.md`+`W20-sslists-cycle-guard.patch`).
5. **TLS (onCreate OkHttp SSLContext, no Conscrypt)** — ✅ CLEARED (TLS shim in adapter-runtime-bcp.jar).
6. **AppCompat theme (`setContentView` → "You need Theme.AppCompat")** — ✅ CLEARED (2026-06-01). TWO fixes, both required: (a) **parser** — `GetIntAttr+TYPE_REFERENCE` in the bridge `liboh_adapter_bridge.so 7446144d` (NOT libapk_installer.so — the launch re-parse runs in the BRIDGE; that was the wrong-`.so` trap) → extracts `appTheme=0x7f14023d`; (b) **propagation** — `[G2.6-THEME]` in `adapter-runtime-bcp.jar 848f414e`: cache `sEnrichedTheme` at bind (`applyManifestFieldsToAppInfoLocal`), re-apply to `activityInfo.theme`+`aiApp.theme` at launch (`nativeOnScheduleLaunchAbility`, after icon-zeroing). The launch `currentApplication().getApplicationInfo()` came back `theme=0x0` (enriched bind appInfo ≠ launch appInfo) → AppCompat threw. Smali-patched + local `dex2oat64` boot regen + deploy-via-reboot. VERIFIED: `AppCompat-err=0`, noice runs past setContentView → fragment inflation → onStart → its OWN `AppIntroActivity` code. Patch: `docs/engine/THEME-G2.6-propagation-HANDOFF.md`.

   **🎉 noice now reaches FOREGROUND** (AMS state 1→2, `foregrounded pState:1`) and runs its own code — the theme wall was the last ADAPTER-substrate wall before noice's app logic.

   **✅ ConnectivityManager stub gap CLEARED (2026-06-01):** batch-added the methods noice's `getInternetConnectivityFlow` calls (source-led from decompiling noice's smali) as "online" functional stubs in `adapter-mainline-stubs.jar 7ef9efa5`: `getNetworkCapabilities`→new caps, `NetworkCapabilities.hasCapability`→true, `getActiveNetworkInfo`→new NetworkInfo, `NetworkInfo.{isConnected,isConnectedOrConnecting}`→true/`isRoaming`→false/`getType`→1/`getSubtype`→0, `isActiveNetworkMetered`→false, `register{Default,}NetworkCallback`/`unregisterNetworkCallback`→no-op, `NetworkRequest$Builder.{addCapability→this,build→new NetworkRequest}`. Boot regen'd + deployed-via-reboot. `NoSuchMethod=0`, `FATAL EXCEPTION=0`.

   **🎉🎉🎉 CANONICAL GOAL MET (2026-06-01): noice's `AppIntroActivity` intro/splash RENDERED + CAPTURED, natively.** Past theme + connectivity, noice reaches FOREGROUND (both `AppIntroActivity` + `MainActivity`), runs full onCreate/onStart/onResume, loads fonts (SkTypeface), draws its surface (`SkiaOpenGLPipeline::swapBuffers #1 drew=1 720x1280`). A burst-snapshot grab caught its **actual welcome slide** — 欢迎 + the **noice** wordmark logo + tagline "通过自然平静的噪音集中注意力、冥想和放松" + Skip/Next intro controls, dark Theme.App, zh-localized. EVIDENCE: `docs/engine/noice-intro-captured-s014.jpeg` (+ `C:\Users\dspfa\Dev\noice-intro-captured.jpeg`, burst archive `noice-burst-2026-06-01.tgz`). This is the unmodified APK's own UI via the real libhwui→Skia→GL pipeline — NOT a mockup. (McD/Netflix never cleared the splash gate.) **CAVEAT: TRANSIENT** — noice still aborts ~10s in on the EGL wall (below), and the keyguard covered it mid-burst, so the intro flashes (captured in 1 frame, s014) rather than staying up. A STABLE/interactive splash needs the EGL fix.

   **🔴 CURRENT WALL (2026-06-01): EGL surface re-creation → libhwui abort (RENDERING wall #9).** After drawing frame #1, a SECOND `eglCreateWindowSurface` on noice's OH NativeWindow ERRORS — `C01400/OpenGLWrapper: egl.eglCreateWindowSurface error.` via the adapter `OH_EglHijack` (ANW-shim→OH-NativeWindow unwrap) → libhwui `ASSERT FAILED ... drawRenderNode called on a context with no surface! (mEglSurface==EGL_NO_SURFACE)` → `abort()`/SIGABRT (NO Java exception, NO cppcrash — a libhwui native abort; pid dies → AMS OnRemoteDied, FOREGROUND=1). Trigger = a surface resize/reuse (`updateSurfaceSize session=11 720x1280` + W21 "session already exists, REUSING") that drops then re-creates the EGL surface, and the re-create fails. ROOT-CAUSED + handoff `docs/engine/EGL-resurface-HANDOFF.md`: after frame #1 the window relayouts → Android surface recreated → hwui's `EglManager` re-creates its EGL surface; the 2nd `eglCreateWindowSurface` returns `EGL_NO_SURFACE`. The hijack is in **libhwui** (`patches/aosp_patches/libs/hwui/hwui_oh_abi_patch.cpp:715` — NOT liboh_adapter_bridge.so; I'd guessed wrong). 2 candidate sub-causes: (a) the W21-reused OH ProducerSurface is still EGL-connected → OH rejects re-connect [fix: `NativeWindowDisconnect(actualWindow)` before the real create — OH API confirmed]; (b) stale OH NativeWindow after relayout [fix: refresh the ANW shim — adapter, user-buildable]. **NEXT: instrument the hijack to log `eglGetError()` (1 line) to disambiguate** → then the fix. **BUILD GATE:** libhwui is an AOSP cross-build; deployed = user's (the user scope-b libhwui.so is a SYMLINK → user's); same access situation as W20 libart → likely OPERATOR build (compile_libhwui.sh exists in scope-b but bionic_compat-blocked for user like libart was — unverified). Prefer the adapter-side fix (b) if the error code points there. **The captured splash (s014) already proves the pipeline; this wall only blocks STABILITY.** Device: HW renders, single appspawn, permissive; libart `56f3caea`, bridge `7446144d`, adapter-runtime-bcp `848f414e` (theme), adapter-mainline-stubs `7ef9efa5` (connectivity), installer `3d4d9d5f`.
7. **Resources/drawables (PREDICTED)** — the `[G2.5]` OH-format resource issue (iconId type-byte 0x00 → NotFoundException, why icons are force-zeroed); theme drawables; layout inflation. **Same root as #6** (adapter resource-ID/AXML resolution).
8. **Room/SQLite native (PREDICTED)** — `androidx.sqlite.db.framework` linking seen in onCreate; noice uses Room. Likely missing native SQLite (`android.database.sqlite`) → provide/shim like TLS.
9. **onResume + window/surface (LIKELY OK)** — HelloWorld renders (`[OH_SurfaceBridge] createSurface`, 720x1280), so the surface pipeline WORKS; noice just needs to reach onResume past #6-8.
10. **FIRST FRAME = THE GOAL (a captured splash).**

**🎯 KEY INSIGHT — the remaining walls cluster into ~3 ROOT efforts (not N empirical walls):**
- **(A) Adapter resource-ID / AXML resolution** → clears #6 (theme) AND #7 (drawables) together. This is the "OH→Android resource-ID translation (P3)" the code itself deferred (`PackageInfoBuilder.java:164`). The single highest-leverage fix.
- **(B) Native SQLite** → clears #8 (Room). "Provide-a-native" pattern (like the TLS/Conscrypt shim).
- **(C) onResume→surface** → likely already works (HelloWorld renders); just need to arrive there.
Plus cross-cutting: stabilize the fork/AMS flakiness (#1) for sane testing.

So "weeks of one-wall-at-a-time" → ~3 systematic efforts, (A) first (highest leverage + unblocks the visible splash).
