---
name: noice-real-blocker-initchild
description: "noice's real blocker IS W20 (real bad_alloc, confirmed per-child stderr) — an ss_lists CYCLE in the substrate's FIX-VTABLE-A. NOT initChild."
metadata: 
  node_type: memory
  type: project
  originSessionId: 422676c7-2c64-4e1a-8602-735c79264cef
---

**CORRECTED 2026-06-01 (supersedes an earlier draft of this file that wrongly said noice dies in initChild / has no bad_alloc).**

noice's launches are **flaky**: some runs die EARLY (pre-bind, e.g. child 16459 — stopped after the AES probe; cause unclear, transient), but **DEEP runs go all the way and die at W20**, which is REAL.

**A deep run (child 18691, on libart 53932c26), measured via the real channel** (hilog `J_*` appLog markers + per-child stderr `/data/service/el1/public/appspawnx/adapter_child_<pid>.stderr`):
- ALL `J_*` markers fire through `J_invokeStaticMain_BEFORE_main_invoke` + `J_probe_complete` → **initChild completes fully**, `ActivityThread.main()` runs.
- `ScheduleLaunchApplication → bindApplication`, `[FIX-W12B/W12E-EARLYACK] AbilityTransitionDone`, AMS `AbilityTransitionDone MainActivity state:1 → state:5 (FOREGROUND)`, **`[B43-BIND] handleBindApplication returned OK`** → noice BINDS + MainActivity reaches FOREGROUND. (So splash-gate + TLS + bind genuinely work — NOT phantoms.)
- Then per-child stderr: a flood of **`[FIX-VTABLE-A-RELOC] ... super[0].name=clone name-mismatch ... iftable-fixups=N`** on the **okhttp3 / kotlinx-coroutines** graph (pulled in by TLS/OkHttp init), then **`[W12F-LINKMETHODS-BADALLOC] CULPRIT: m8.e nvirt=13 what=std::bad_alloc`**.

**THE RUNAWAY (root-caused + FIXED 2026-06-01)** — NOT the arena containers I guarded earlier (those `W20-FIX` guards fired 0 times). It's `class_linker.cc` FIX-VTABLE-A pass ~8996: walking the `same_signature_vtable_lists` (`ss_lists`) chain into a **`std::vector<uint16_t> slots` (operator new — matches the allocprobe's 128M→1G doubling)**. Only termination check is `chain_idx >= ss_lists.size()` (OOB) — a **CYCLE** in `ss_lists` (creatable by the clone-name-mismatch relocations) stays in-bounds → `slots.push_back` forever → 1GB → bad_alloc. **✅ FIX VERIFIED: ss_lists cycle guard** (cap walk at `ss_lists.size()` + break + `[W20-FIX] ss_lists cycle` log). Built `56f3caea` (`$HOME/libart-pathA-work`, patch already in `src/class_linker.cc`; `bash build_libart_pathA.sh`), deployed `/system/android/lib/libart.so`. Post-reboot deep run: `W20FIX-cycle=2` (fired on BOTH v8.e and m8.e, len=11 primary=0 cycles), **`BADALLOC=0`**, linking proceeds 1415 relocs deep. W20 was the Netflix/Spotify/Maps wall — engine-wide fix. **PACKAGED (operator): `docs/engine/W20-OPERATOR-HANDOFF.md` + `W20-sslists-cycle-guard.patch` (validated forward/reverse-applies to `art/runtime/class_linker.cc`); the old `W20-class_linker-guard.patch` is renamed `.SUPERSEDED-wrong-function` (guarded the wrong fn, fired 0×).**

**NEW FRONTIER (post-W20, 2026-06-01) = AppCompat THEME — `MainActivity.onCreate` RUNS!** Pinned via hilog (C00f00/AppSpawnXInit): noice reaches `MainActivity.onCreate(MainActivity.kt:128)` → `AppCompatActivity.setContentView` → `AppCompatDelegateImpl.P(:671)` throws **`java.lang.IllegalStateException: You need to use a Theme.AppCompat theme (or descendant) with this activity`** → `J_invokeStaticMain_main_threw` (uncaught) → process self-exits (~8s; that's why `killId=-1`, no OOM [1.2GB free], no tombstone — it's a clean Java uncaught-exception exit; AMS's later `foreground timeout` is just a consequence). NOT memory/timeout. **ROOT: `bm dump` shows MainActivity `"theme": ""` (empty)** — the manifest theme is parsed (`apk_manifest_jni.cpp:117 j["appTheme"]=m.appTheme`, logs `theme=0x%x`) but DROPPED before the installed ActivityInfo → Activity falls back to a non-AppCompat default → AppCompat's `obtainStyledAttributes(R.styleable.AppCompatTheme)` finds no `windowActionBar` etc. → throws. **FIX LOCATION FOUND:** the theme IS parsed (`apk_manifest_parser.cpp:300/362`) + set at launch (`AppSchedulerBridge.java:1129 ai.theme=appTheme` from the manifest re-parse), but the **`[G2.5-SLA]` block (AppSchedulerBridge.java:1394 `activityInfo.theme=0` + 1402 `aiApp.theme=0`; smali AppSchedulerBridge.smali:6232/6255)** force-zeroes it.

**⚠️ THEME WALL — ROOT IS THE MANIFEST PARSER (theme=0x0), NOT the [G2.5-SLA] zeroing (2026-06-01, corrected).** noice DOES have themes (decoded `base.apk`: `<application android:theme="@style/Theme.App">`, MainActivity `android:theme="@style/Theme.Material3.Dark.NoActionBar"` — Material3 IS an AppCompat descendant → would satisfy the check). BUT the native parser logs **`OH_ApkManifestJni nativeParseApkManifestJson: theme=0x0`** and `[G2.5-SLA-PRE] activityInfo theme=0x0 | appInfo theme=0x0` — **`apk_manifest_parser.cpp` `GetIntAttr(android:theme)` returns 0 instead of the `@style` resource ID** (doesn't resolve the reference). So `ai.theme` is never set → Activity themeless → AppCompat throws. **The un-zero smali fix (`AppSchedulerBridge` 6232/6255, jar `337c8a55`, deployed) is NECESSARY-BUT-INSUFFICIENT — un-zeroing a value that's already 0x0 is a no-op.** Complete fix = (a) **fix the manifest parser to extract `android:theme`** + (b) the un-zero (done, smali jar `337c8a55`).

**❌❌ PARSER FIX ATTEMPT #1 FAILED (2026-06-02): `GetIntAttr` + `TYPE_REFERENCE` did NOT work — `theme=0x0` STILL.** Built libapk_installer.so `18474f1d` ([build-host] `compile_apk_installer.sh`; patch `docs/engine/THEME-fix-manifest-parser-GetIntAttr.patch`), deployed `/system/lib` via reboot, confirmed loaded (launchMode parse still works). But `nativeParseApkManifestJson: ...noice... theme=0x0` UNCHANGED; noice still throws AppCompat (err=5, main_threw=1, dies). `ATTR_THEME=0x01010000` is correct (theme attr IS found, like launchMode 0x0101001d which reads fine), so the real bug is **`axml_parser.cpp getAttributeValue` returns `data=0` for the `@style` theme attr** (a TYPE_REFERENCE / 0x7f.. app-resource ref) while returning `data=2` for launchMode (TYPE_INT_DEC). Both noice's app theme (@style/Theme.App) and MainActivity theme (@style/Theme.Material3.Dark.NoActionBar) decode to 0. **NEXT (attempt #2): INSTRUMENT the parser** — log the theme attr's `idx`, `value.dataType`, `value.data` in `GetIntAttr`/`getAttributeValue` → find why `@style`/0x7f references decode to 0 (suspects: FindAttrByResId returns wrong idx for theme; getAttributeValue mis-reads the typedValue for references; AXML attr-entry stride). Another [build-host] build to add logging, then the real fix. Patches: `THEME-fix-AppSchedulerBridge.patch` (un-zero, valid) + `THEME-fix-manifest-parser-GetIntAttr.patch` (INEFFECTIVE — keep but it's not the fix) + `THEME-FIX-HANDOFF.md`.

**VERIFICATION ALSO BLOCKED:** noice's child forks reliably only ~once per reboot (`kill -9 helloworld` disrupts appspawn → noice won't fork); HelloWorld's `createSurface` render-loop floods hilog (rolls out noice's parse/bind logs — capture EARLY ~5s); `bm install -p` reinstall FAILS (`install internal error` 9568260) so bm-dump-theme check is blocked too. To read the theme value: forking run + early hilog capture of `nativeParseApkManifestJson ... theme=0x?`.

**🔑🔑 MEASUREMENT (3rd time burned — INTERNALIZE): the per-child stderr FILE (`adapter_child_*.stderr`) is BUFFERED → it LOSES the Java uncaught exception / `J_invokeStaticMain_main_threw` on abrupt death. byw1r3d7a's `AppCompat-err=0` (grep on the stderr FILE) was a FALSE NEGATIVE — the reliable HILOG (`C00f00/AppSpawnXJava [stderr]` mirror + `C00f00/AppSpawnXInit`) showed the AppCompat error STILL fired.** For app Java exceptions/death use HILOG (the `[stderr]`→hilog mirror), NOT the child-stderr file. Native `fprintf(stderr)` logs (FIX-VTABLE-A, [W20-FIX], [W12F-LINKMETHODS-BADALLOC]) ARE flushed → reliable in the child stderr file (so W20's verification stands). The "noice reaches sqlite past AppCompat" was also wrong — that sqlite linking was the Application-bind Room init (before the Activity's setContentView), not proof of passing AppCompat.

**Deploy lesson (stands):** boot-image/BCP-jar changes → deploy via REBOOT (single appspawn-x). `deploy_tls.sh`'s `kill -9 appspawn-x`+`start_asx` can leave TWO appspawn-x (`pidof` shows 2 pids) → socket conflict → all launches fail (looks like a fix regression). Build path (local, no [build-host]): baksmali→edit smali→SmaliAsm (`/tmp/w13`)→`regen_tls.sh`→install /system→REBOOT. Device: jar `337c8a55` + libart `56f3caea` (W20, SOLID) + installer `3d4d9d5f` + B49 `8bb53fc8`, HW renders; noice fails AppCompat (theme=0x0).

**How to test correctly:** force-stop + settle (~4s, else AMS drops the relaunch), single launch, then read the per-child stderr (newest `adapter_child_*.stderr`) for `[W20-FIX]` (cycle guard fired) and absence of `[W12F-LINKMETHODS-BADALLOC]`. Need a DEEP run (binds) to exercise the path — flaky, so retry. Then look for `createSurface` / a rendered frame. See [[measurement-channels]].
