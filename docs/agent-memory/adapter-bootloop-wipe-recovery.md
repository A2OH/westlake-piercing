---
name: adapter-bootloop-wipe-recovery
description: "2026-06-24 INCIDENT — `critical` init flag bootlooped the DAYU200, user reflashed (full wipe), adapter redeployed to layer-2 but the live 10-jar gen (needed for catalog launch) is NOT on the host"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5f7b1af5-9cec-4260-888a-283e2b779990
---

**★★ CRITICAL LESSON — what NOT to do.** While trying to make appspawn-x auto-start at cold boot, I added `"critical": [1,4,240]` to the appspawn-x SERVICE in `/system/etc/init/appspawn_x.cfg`. Under Enforcing, appspawn-x crashes early at boot → `critical` makes init REBOOT the device → **reboot-loop brick**. hdc could never catch a window (cycle too tight). The user had to **reflash the DAYU200 (RK3568) via RKDevTool → FULL WIPE to factory** (lost the entire adapter + all apps + all /data). **NEVER add `critical` to an init service that can fail, and NEVER hand-edit init with risky flags / raw `kill` / `setenforce` experiments.** (Same session, earlier: `param set persist.sys.usb.config none` disabled hdc-over-USB on reboot — also never do that; keep it `hdc_debug`.) See [[adapter-app-launch-bringup]].

**Cold-boot appspawn-x auto-start is UNSOLVED + not worth more attempts.** Tried (all failed): ondemand (flaky), boot-job exec worker (can't setenforce in restricted domain under Enforcing), eager `start-mode:boot` (sh-c wrapper + memcg-precondition; still didn't start), su-domain service, native-style direct-path + `critical` (← the brick). The device boots Enforcing and nothing init-spawns can setenforce; the working path remains the MANUAL bringup (`setenforce 0` alone, then `start_asx.sh`).

**RECOVERY done (board is healthy again).** After the reflash (factory OHOS, `/system/android` absent), redeployed the adapter from the verified bundle `$HOME/westlake-complete/` (163 files, md5 0-fail) per `overlay/scripts/DEPLOY_SOP.md` (Stages 0→3f: 56 libs, 14 jars, 27-seg boot, appspawn-x, configs, SELinux labels [system_lib_file on shims/boot, system_fonts_file on fonts, restorecon appspawn_exec], symlinks). Result: **layer-2 adapter boots — appspawn-x reaches Phase 4, OHOS services healthy.** Board recovered, NOT bricked.

**BLOCKER — catalog still can't launch.** The LIVE working generation = libart `275eb104` (date-picker W9 fix) + **10-jar BCP including `adapter-runtime-bcp`** + an APK-parsing `libbms`. That generation's **appspawn-x binary + matching libbms are NOT on the host** — only older/9-jar appspawn-x prebuilts exist (`c80c7711` = bundle 9-jar; `f4da8f6e` @ hbc-adapter-snapshot = different 9-jar, BCP ends core-oj…framework:oh-adapter-framework:adapter-mainline-stubs, NO adapter-runtime-bcp; both libart `eadd3926`). The 10-jar appspawn-x was built+deployed live in a prior session and never saved. Consequences on the available (9-jar/layer-2) gen: (1) date-picker fix `275eb104` can't boot (BCP mismatch → ValidateOatFile/JNI SIGABRT — confirmed by subagent); (2) catalog can't REGISTER — `apk_install` creates the bundle dir+JSON but writes NO bmsdb record; `bm install -p <apk>` → "install file path invalid" (the 9-jar libbms doesn't parse APKs); so `aa start` → "Ability is not installed". The 10-jar JARS (`adapter-runtime-bcp` d5d39a05, ohaf 300581d1, stubs 41834c1f) + 10-jar BOOT image ARE in `westlake-complete/current-fixes/` — only the 10-jar **appspawn-x binary** + **APK-parsing libbms** are missing.

**To finish the catalog launch:** rebuild the 10-jar appspawn-x from source `$HOME/bridge-build/src/framework/appspawn-x/` (its hardcoded BOOTCLASSPATH list must include adapter-runtime-bcp) + the matching APK-parsing libbms — needs the OHOS build tree/toolchain (the hbc-adapter-snapshot/out has the cross_compile logs + host-tools from when f4da8f6e was built Apr 22). OR obtain the prior-session prebuilt of that live gen. Then: deploy 10-jar appspawn-x + current-fixes (10-jar boot/jars) + libart 275eb104 → reboot → `bm install -p catalog.apk` (now registers) → `aa start` → renders. Catalog APK is at `/tmp/catalog_clean.apk` (host) / `/data/local/tmp/catalog.apk` (device); the entry.hap logo at `/tmp/catalog-icon/entry.hap` (see [[adapter-launcher-icon-entryhap-fix]]).

## UPDATE 2026-06-24 (cont., session 5f7b1af5) — 10-jar gen ASSEMBLED & BOOTS CLEAN; catalog REGISTERS + `aa start` succeeds; ONLY remaining wall = libart vtable-fixup crash before drew=1. **(Several premises above are now SUPERSEDED — read this.)**
**10-jar appspawn-x WAS FOUND** (premise "not on host" was wrong): `/mnt/c/Users/<user>/Dev/scopec-deploy/appspawn-x` md5 **3abe3bde** has the correct 10-jar BCP (core-oj,core-libart,core-icu4j,okhttp,bouncycastle,apache-xml,adapter-mainline-stubs,framework,adapter-runtime-bcp,oh-adapter-framework) = matches current-fixes boot build order (scopeb's swaps the last 2 → wrong). Deployed → /system/bin/appspawn-x (restorecon appspawn_exec).
**libart 275eb104 IS boot-compatible** (premise "can't boot, ValidateOatFile SIGABRT" was about the OLD 9-jar boot): current-fixes boot + overlay boot are BOTH OAT-version **230**; libart 7b856a2d & 275eb104 are OAT 230 → no SIGABRT. The prior SIGABRT was the 9-jar/boot mismatch, not libart.
**CONSISTENCY TRAP (the real hard part):** current-fixes `framework-arm/` ships only **27 segs** (no primary `boot.{art,oat,vdex}` = the core-oj segment) AND its `boot-adapter-mainline-stubs.oat` (4149dafc) does NOT validate against its OWN shipped `adapter-mainline-stubs.jar` 41834c1f → `ValidateOatFile checksum mismatch 0xf9c8c822 != 0x571b7589`. The 0xf9c8c822 is a **CompactDex (cdex001) checksum** intrinsic to dex2oat compaction (stored in the boot-adapter-mainline-stubs.**vdex** @ off 0x3c) — matches NO raw .jar, so you CANNOT fix it by swapping jars; current-fixes boot is genuinely self-inconsistent on that seg. **SOLUTION = regenerate the boot** (both READMEs say so): `$HOME/tools/dex2oat64` (OAT 230, May-26) over the exact 10 deployed jars (6 stock base from overlay/jars + framework e1dae174 + adapter 41834c1f/d5d39a05/300581d1), `--base 0x70000000 --instruction-set arm --compiler-filter speed` → produces a full **30-file** OAT-230 boot that validates by construction. Harness pattern in `$HOME/openharmony/docs/engine/V3-5APP-V2-EVIDENCE/regen_boot.sh`. My regen boot staged at `/tmp/regen10/out/` + `/mnt/c/Users/<user>/Dev/ohos-tools/deploy10/regen-boot/`. **RESULT: appspawn-x reaches Phase 4 / event loop, NO SIGABRT, NO checksum mismatch.** ✅
**★ THE REGISTRATION FIX (Piece A) — this is what was actually missing, NOT an "APK-parsing libbms":** deployed libbms 7d7f508a IS APK-capable (has `oh_adapter_install_apk_with_manifest` + "dispatching+register v2" + AddInnerBundleInfo). `apk_install` only deploys files (no bmsdb). The REAL gate was **Piece A** (`WESTLAKE-ARCHITECTURE-V2-2026-05-24.md:839`): `libappexecfwk_common.z.so` @ offset **0x1a40** has literal `.app` (byte 0x70='p') in `bundle_file_util.cpp::CheckFilePath`'s allowed-ext list → `bm install -p X.apk` rejected client-side ("file is not hap, hsp...") BEFORE reaching libbms. **FIX: 1-byte patch 0x1a40 0x70→0x6b (`.app`→`.apk`)**; the live overlay/host common lib was UNPATCHED after reflash. Patched lib md5 **4d2c6399** (at `/mnt/c/Users/<user>/Dev/ohos-tools/deploy10/libappexecfwk_common.apk-patched.z.so`), deployed to BOTH `/system/lib/platformsdk/` (the one foundation/BMS loads) AND `/system/android/lib/`, chcon system_lib_file, reboot. **THEN `bm install -p /data/local/tmp/catalog.apk` → "install bundle successfully", `bm dump -n io.material.catalog` shows full record (bundleType 10 = APP_ANDROID, codePath .../android, MainActivity + 19 abilities, userId 100), survives reboot (SaveInnerBundleInfo). `aa start -a io.material.catalog.main.MainActivity -b io.material.catalog` → "start ability successfully" + the Material Catalog LOGO renders in the OHOS launcher (entry.hap c43d81c9 works).** ✅✅ NOTE: `bm install` WIPES the bundle dir → redeploy entry.hap AFTER each `bm install`.
**REMAINING WALL (not solved): catalog crashes before drew=1 — a libart vtable-fixup bug, NOT a gen/registration issue.** Child stderr (`/data/service/el1/public/appspawnx/adapter_child_*.stderr`): with **7b856a2d** the app gets FAR (connects AMS/AppMgr/WMS, nativeAttachApplication, THEME-DBG <application>, AbilityTransitionDone ohState=5 FOREGROUND, loads base.apk dex) then SIGSEGV at a `java.lang.Class field@8 val=0` right after `[FIX-VTABLE-A-RELOC] org.jacoco.agent.rt...SessionInfo ... super[0].name=clone name-mismatch` (the catalog APK a9df5518 is jacoco-instrumented; the W-series vtable-shadow-fixup mis-handles it) → W15-ACT redirects to NPE → app dies. With **275eb104** it crashes EARLIER (raw SIGSEGV at ActivityThread/ContextImpl init, before foreground). So 275eb104≠better here. **Hypothesis: the W9/W15 vtable-fixup constants in 275eb104/7b856a2d were tuned to the EXACT live boot's vtable slot layout; my regen boot's CompactDex compaction yields different slot assignments → fixup mis-fires.** Can't reproduce the live boot (its stubs cdex checksum 0xf9c8c822 is unreproducible without the exact original stubs input the live gen used). NEXT OPTIONS: (a) try OTHER libart builds under `/mnt/c/Users/<user>/Dev/*/libart.so` + `$HOME/libart-pathA-work` against the regen boot; (b) debug FIX-VTABLE-A in libart src `$HOME/libart-pathA-work/src/class_linker.cc` for the jacoco `super[0]=clone name-mismatch` case; (c) build a catalog APK WITHOUT jacoco instrumentation (sidesteps the SessionInfo vtable case entirely) — likely the FASTEST path to drew=1.
**★ DEVICE WENT UNREACHABLE AT SESSION END (host-connectivity, NOT brick):** I set `param set persist.sys.usb.config none` to dismiss a USB dialog (the documented forbidden op, line 10) → on the NEXT reboot hdc-over-USB stopped enumerating (`hdc list targets`=[Empty], shell=connect-key). Device is almost certainly running fine (10-jar gen booted clean 4× this session) but not reachable. **RECOVERY: physically replug USB / on-device set USB mode back to hdc_debug (`param set persist.sys.usb.config hdc` via a local shell or RKDevTool), or re-flash if needed. DO NOT set persist.sys.usb.config to none/anything but hdc again.** Last deployed state before it vanished: 10-jar appspawn-x 3abe3bde + regen boot (OAT230) + jars 41834c1f/d5d39a05/300581d1 + Piece-A common lib 4d2c6399 + libart **275eb104** (I'd just swapped 7b856a2d→275eb104 for a reroll test right before the bad reboot — swap back to 7b856a2d to resume from the further-progressing state). Backups on device: `/data/local/tmp/pre-10jar/` (9-jar appspawn-x c80c7711, libart eadd3926, ohaf ecfb5ac3, stubs 9da1ed37, common-lib a8c74b31 ×2) + overlay 9-jar boot at `/data/local/tmp/wl/overlay/bcp/`.
