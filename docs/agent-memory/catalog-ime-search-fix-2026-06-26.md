---
name: catalog-ime-search-fix-2026-06-26
description: "2026-06-26 deep dive on catalog Search. (1) FIXED the 2nd-Activity crash (SearchRecyclerDemo etc. froze, LIFECYCLE_TIMEOUT) — bridge 39179c16: createSession abilityName discriminator routes a new-ability window as a MAIN window instead of a hidden SUB-window. (2) Typed-text INTO a 2nd-Activity search field is BLOCKED — the fix lives in OhImeBridge (ohaf), but ohaf is a boot-image BCP jar so any modification aborts ART init → needs a full boot-image regen (ECS-only tooling). User banked the crash fix. Demo-stable config recorded."
metadata:
  node_type: memory
  type: project
  originSessionId: 5f7b1af5-9cec-4260-888a-283e2b779990
---

2026-06-26 (cont.): chased catalog Search end-to-end. NET: the CRASH is fixed + deployed; typing INTO 2nd-Activity search fields is blocked behind a boot-image rebuild. (Supersedes this file's earlier "search IME FIXED" — that was the toolbar-summon only.)

**★ FIXED — 2nd-Activity crash (bridge 39179c16, deployed):** user's "recycle view demo brought up IME but can't input + app crashed" = `io.material.catalog.search.SearchRecyclerDemoActivity` (a SEPARATE Activity) froze on foreground (`LIFECYCLE_TIMEOUT`). ROOT CAUSE (top-down, confirmed via `hidumper -s WindowManagerService -a -a`): `createSession` (oh_window_manager_client.cpp) routed EVERY 2nd app-main window as a hidden SUB_WINDOW (Type 1001, ZOrd -1) — the 2026-06-04 translucent-dialog logic applied UNCONDITIONALLY. So a real 2nd Activity never became visible (mViewVisibility=GONE, mAppVisible=false), its AMS starting window (Pid 0, Type 1) was never replaced → AAFWK foreground watchdog killed it. FIX: an **abilityName discriminator** — route as SUB_WINDOW only if the new window's abilityName == the foreground main's (a dialog/bottom-sheet within the SAME activity); a DIFFERENT abilityName (a new top-level Activity) becomes its own MAIN window. Added `abilityName` to SessionEntry (oh_window_manager_client.h). **bridge md5 39179c16** (host: $HOME/bridge-build/out/adapter/liboh_adapter_bridge.so). Build: `ADAPTER_ROOT=$HOME/bridge-build OH_ROOT=$HOME/openharmony AOSP_ROOT=$HOME/bridge-build/aosp BUILD_INNER_INVOKED=1 bash build/inner/compile_oh_adapter_bridge.sh`. VERIFIED: SearchRecyclerDemo foregrounds (WMS Focus = its MAIN win, ZOrd 1), renders the search screen + suggestions, toolbar IME summons + dismisses; dialogs not regressed (same-ability path byte-identical). Deploys with NO boot regen (a .so isn't BCP).

**✗ BLOCKED — typed text doesn't land in a 2nd-Activity search field:** routing is `OhImeBridge.focusedInputConnection()` (Java, in **ohaf**=oh-adapter-framework.jar). Patch pipeline WORKS: javac vs android-34 with `-XDstringConcat=inline` (avoid invokedynamic synthetics) → d8 `--min-api 28` (dex 039) → FAITHFUL dexlib2 surgical class-swap keeping the other 52 classes byte-identical (`/tmp/fwktools/ReplaceClass.java`, compile vs `android-sdk/cmdline-tools/latest/lib/external/com/android/tools/smali/smali-dexlib2/3.0.3/smali-dexlib2-3.0.3.jar` + guava-31.1). **BUT ohaf is a BOOT-IMAGE BCP jar** (boot-regen lists `boot-oh-adapter-framework`): deploying ANY modified ohaf aborts ART at startup — `art::Runtime::Abort` ← `ClassLinker::CheckSystemClass` ← `Runtime::Init` ← `JNI_CreateJavaVM` — because boot.oat is checksum-bound to the original ohaf (PROVEN: 3 variants incl. a byte-faithful one all aborted identically). Shipping a modified ohaf REQUIRES a full boot-image regen: dex2oat64 (`$HOME/tools/dex2oat64`, md5 388602ec) + all 9 BCP jars + atomic 27-seg `gen_boot_image.sh`, coupled to device libart 275eb104; scripts say "ECS only". Local snapshot `android-to-openharmony-migration/hbc-adapter-snapshot/out/` is a DIFFERENT baseline (ohaf e8a4ef6f ≠ device 4690cae1). The input-routing CAUSE is still UNDIAGNOSED (instrumented ohaf can't run). User chose (b): bank the crash fix.

**★ DEMO-STABLE CONFIG (loaded + validated 2026-06-26):** bridge **39179c16** + ohaf **4690cae1** + libart **275eb104** + libhwui **1d04a56e** + fontcfg **425290bd** + helper **60d9c15f** + catalog bundle registered + entry.hap + Permissive. Host crash-fix libs: `$HOME/bridge-build/out/adapter/`. Boot backup: `/data/local/tmp/boot-full-bak/arm/` (30 segs); orig ohaf backup `/data/local/tmp/ohaf-pre-icfix.jar`. ⚠ the older `catalog-demo-FULL-bak/restore.sh` redeploys bridge **9b2a9727** (PRE-crash-fix) — after any reflash, redeploy **39179c16** from the host out/ to keep the crash fix. Demo: cold boot → swipe unlock → tap Material Catalog → navigate; separate-Activity demos now OPEN instead of crashing; don't type in the in-demo search fields (text won't land — only the MainActivity toolbar search accepts input). Related: [[catalog-2nd-level-canvascontext-wall]], [[catalog-reflash-restore-2026-06-26]], [[catalog-ime-bridge-impl]].
