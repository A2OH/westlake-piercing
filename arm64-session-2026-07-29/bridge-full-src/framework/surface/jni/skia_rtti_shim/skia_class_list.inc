// skia_class_list.inc — generated by discover_skia_rtti_syms.sh (Round 2)
// Source: readelf --syms ~/adapter/out/hwui-build/obj/*.o on 2026-04-12
// Ground truth: exact set of _ZTI* symbols left UND by 147 libhwui .o files.
//
// Flat classes (7): produced by SKIA_RTTI_CLASS macro in skia_rtti_shim.cpp.
// Nested classes (1): declared inline below because the macro can't handle
// nested mangling directly.
//
// Do not hand-edit: re-run discover_skia_rtti_syms.sh when the libhwui
// .o set or Skia header layout changes.

// ---- flat Sk* classes ----
SKIA_RTTI_CLASS(SkCanvas)
SKIA_RTTI_CLASS(SkNoDrawCanvas)
SKIA_RTTI_CLASS(SkPaintFilterCanvas)
SKIA_RTTI_CLASS(SkDrawable)
SKIA_RTTI_CLASS(SkPixelRef)
SKIA_RTTI_CLASS(SkWStream)
SKIA_RTTI_CLASS(SkExecutor)

// ---- nested: SkBitmap::{Allocator, HeapAllocator} ---------------------
// Symbols emitted:
//   _ZTIN8SkBitmap9AllocatorE      (base class — NEW 2026-04-12)
//   _ZTIN8SkBitmap13HeapAllocatorE (direct hwui subclass consumer)
//
// SkBitmap is a non-polymorphic value type in real Skia — intentionally NOT
// given a virtual destructor here so the compiler does NOT emit _ZTI8SkBitmap
// (which real Skia doesn't export either).
//
// Why both Allocator AND HeapAllocator:
//   - Audit 2026-04-12 on libhwui sibling modules (JNI under frameworks/
//     base/libs/hwui/jni/) found multiple classes that inherit directly from
//     SkBitmap::Allocator, NOT from HeapAllocator:
//       * RecyclingPixelAllocator       (jni/BitmapFactory.cpp:131)
//       * AshmemPixelAllocator          (jni/GraphicsJNI.h:257)
//     plus indirect consumers via the adapter's BRDAllocator compat header:
//       * BRDAllocator : SkBitmap::Allocator   (build/skia_compat_headers/BRDAllocator.h)
//       * HeapAllocator : BRDAllocator         (jni/GraphicsJNI.h:177)
//       * RecyclingClippingPixelAllocator : BRDAllocator (jni/GraphicsJNI.h:223)
//   - Those JNI .cpp files are not yet in the currently-linked libhwui .o
//     set (compile_libhwui_jni.sh doesn't pull them in as of today), so the
//     current readelf-based discovery pass does NOT report SkBitmap::Allocator
//     as undefined. But the moment JNI coverage expands, the symbol demand
//     will appear. Adding it now avoids a latent gap.
//   - HeapAllocator is kept even though no current .o references it directly,
//     because the discovery pass captured it from an earlier link attempt.
//     Keeping both is zero overhead (~30 bytes) and protects against regression
//     if the upstream JNI files change their inheritance chain.
class __attribute__((visibility("default"))) SkBitmap {
public:
    class __attribute__((visibility("default"))) Allocator {
    public:
        virtual ~Allocator();
    };
    class __attribute__((visibility("default"))) HeapAllocator {
    public:
        virtual ~HeapAllocator();
    };
};
SkBitmap::Allocator::~Allocator() {}
SkBitmap::HeapAllocator::~HeapAllocator() {}
